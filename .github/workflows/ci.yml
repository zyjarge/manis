name: ci

# Fast sanity check — runs on every PR + push to main, builds the
# project for the host platform only (no packaging, no upload). If
# this fails, the tag-triggered release workflow won't work either.
on:
  pull_request:
  push:
    branches: [main]

jobs:
  smoke:
    name: Smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            label: macOS
            script: build-mac.sh
          - os: windows-latest
            label: Windows
            script: build-windows.sh
          - os: ubuntu-22.04
            label: Linux
            script: build-linux.sh

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest

      - name: Install GTK WebKit runtime libs (Linux)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-0 \
            libgtk-3-0 \
            libnotify4 \
            libsecret-1-0 \
            libsoup-3.0-0

      - name: Install Python (skip on macOS — uses system framework Python)
        if: matrix.os != 'macos-latest'
        run: uv python install 3.13

      - name: Build (no packaging)
        shell: bash
        run: |
          chmod +x scripts/${{ matrix.script }}
          # macOS uses the system framework build of Python 3.13 from
          # Homebrew; other runners use uv-managed Python 3.13.
          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            PYTHON_BIN="$(brew --prefix python@3.13)/bin/python3.13" \
              ./scripts/${{ matrix.script }}
          else
            ./scripts/${{ matrix.script }}
          fi